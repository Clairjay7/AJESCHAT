<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Inset the AJES logo so adaptive icon masking doesn't crop it -->
    <item
        android:drawable="@drawable/ajes_logo"
        android:top="16dp"
        android:bottom="16dp"
        android:left="16dp"
        android:right="16dp" />
</layer-list>
